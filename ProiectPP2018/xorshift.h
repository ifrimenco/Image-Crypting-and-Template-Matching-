#ifndef XORSHIFT_H_
#define XORSHIFT_H_

unsigned int rng(unsigned int);
unsigned int* gen_sir_random(unsigned int, unsigned int);
unsigned int* generare_permutare(unsigned int, unsigned int*);
unsigned int* gen_perm_inv(unsigned int, unsigned int*);

#endif // XORSHIFT_H_
